<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:g="urn:import:com.google.gwt.user.client.ui">
 <ui:style>
    .outer {
      padding: 10px;
    }
    .label {
      font-weight: bold;
    }
    .value {
      padding: 4px 0px 15px 15px;
    }
  </ui:style>

  <g:HTMLPanel debugId="boundElementHolder" styleName="{style.outer}">
    <div id="id">
            <div class="{style.label}">Id</div>
            <div class="{style.value}" ui:field="id"/>
        </div>
    
    <div id="created">
            <div class="{style.label}">Created</div>
            <div class="{style.value}" ui:field="created"/>
        </div>
    <div id="skill">
            <div class="{style.label}">Skill</div>
            <div class="{style.value}" ui:field="skill"/>
        </div>
    <div id="student">
            <div class="{style.label}">Student</div>
            <div class="{style.value}" ui:field="student"/>
        </div>
    <div id="skillLevel">
            <div class="{style.label}">Skill Level</div>
            <div class="{style.value}" ui:field="skillLevel"/>
        </div>
    <div id="version">
            <div class="{style.label}">Version</div>
            <div class="{style.value}" ui:field="version"/>
        </div>

    <g:Button ui:field="delete">Delete</g:Button>
  </g:HTMLPanel>
</ui:UiBinder>
