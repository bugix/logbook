<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:g="urn:import:com.google.gwt.user.client.ui">
 <ui:style>
    .outer {
      padding: 10px;
    }
    .label {
      font-weight: bold;
    }
    .value {
      padding: 4px 0px 15px 15px;
    }
  </ui:style>

  <g:HTMLPanel debugId="boundElementHolder" styleName="{style.outer}">
    
    
    
    

    <g:Button ui:field="delete">Delete</g:Button>
  <div id="id">
            <div class="{style.label}">Id</div>
            <div class="{style.value}" ui:field="id"/>
        </div>
        <div id="levelNumber">
            <div class="{style.label}">Level Number</div>
            <div class="{style.value}" ui:field="levelNumber"/>
        </div>
        <div id="skills">
            <div class="{style.label}">Skills</div>
            <div class="{style.value}" ui:field="skills"/>
        </div>
        <div id="skillAcquired">
            <div class="{style.label}">Skill Acquired</div>
            <div class="{style.value}" ui:field="skillAcquired"/>
        </div>
        <div id="version">
            <div class="{style.label}">Version</div>
            <div class="{style.value}" ui:field="version"/>
        </div>
    </g:HTMLPanel>
</ui:UiBinder>
