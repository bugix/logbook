<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder" xmlns:g="urn:import:com.google.gwt.user.client.ui">
 <ui:style>
    .outer {
      padding: 10px;
    }
    .label {
      font-weight: bold;
    }
    .value {
      padding: 4px 0px 15px 15px;
    }
  </ui:style>

  <g:HTMLPanel debugId="boundElementHolder" styleName="{style.outer}">
    <div id="id">
            <div class="{style.label}">Id</div>
            <div class="{style.value}" ui:field="id"/>
        </div>
    <div id="studentId">
            <div class="{style.label}">Student Id</div>
            <div class="{style.value}" ui:field="studentId"/>
        </div>
    <div id="shib_id">
            <div class="{style.label}">Shib_id</div>
            <div class="{style.value}" ui:field="shib_id"/>
        </div>
    <div id="email">
            <div class="{style.label}">Email</div>
            <div class="{style.value}" ui:field="email"/>
        </div>
    <div id="gender">
            <div class="{style.label}">Gender</div>
            <div class="{style.value}" ui:field="gender"/>
        </div>
    <div id="name">
            <div class="{style.label}">Name</div>
            <div class="{style.value}" ui:field="name"/>
        </div>
    <div id="preName">
            <div class="{style.label}">Pre Name</div>
            <div class="{style.value}" ui:field="preName"/>
        </div>
    <div id="studentStatus">
            <div class="{style.label}">Student Status</div>
            <div class="{style.value}" ui:field="studentStatus"/>
        </div>
    <div id="studyYear">
            <div class="{style.label}">Study Year</div>
            <div class="{style.value}" ui:field="studyYear"/>
        </div>
    <div id="skillAcquired">
            <div class="{style.label}">Skill Acquired</div>
            <div class="{style.value}" ui:field="skillAcquired"/>
        </div>
    <div id="version">
            <div class="{style.label}">Version</div>
            <div class="{style.value}" ui:field="version"/>
        </div>

    <g:Button ui:field="delete">Delete</g:Button>
  </g:HTMLPanel>
</ui:UiBinder>
